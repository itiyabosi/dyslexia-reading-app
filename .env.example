# サーバー設定
PORT=3001

# データベース設定
# PostgreSQL Database URL (本番環境)
# Renderなどのクラウドサービスでは自動的に設定されます
DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# ローカル開発用のPostgreSQL接続例
# DATABASE_URL=postgresql://postgres:password@localhost:5432/dyslexia_reading

# SQLite設定（非推奨：本番環境では使用しないでください）
# DATABASE_PATH=./reading_data.db

# 開発モード設定
NODE_ENV=development

# Firebase連携設定（オプション）
# Firebase Consoleでプロジェクトを作成し、サービスアカウントキーをダウンロードしてください
# JSONファイルの内容を1行の文字列として貼り付けてください
# FIREBASE_SERVICE_ACCOUNT='{"type":"service_account","project_id":"your-project-id",...}'
#
# Realtime Database を使用する場合（Firestoreのみの場合は不要）
# FIREBASE_DATABASE_URL=https://your-project-id.firebaseio.com
